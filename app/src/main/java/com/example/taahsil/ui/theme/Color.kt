package com.example.taahsil.ui.theme

import androidx.compose.ui.graphics.Color

// Modern Light Blue / Pink / White palette
val DeepNavy = Color(0xFF2B5EA7)       // Deep Blue (primary container)
val ElectricBlue = Color(0xFF5BB5F0)   // Sky Blue (primary accent)
val Emerald = Color(0xFFF48FB1)        // Soft Pink (secondary accent)
val OffWhite = Color(0xFFFAFBFF)       // Snow White (light background)
val BorderLight = Color(0xFFE0E4F0)    // Soft Lavender (borders)
val DarkBackground = Color(0xFF101827) // Midnight (dark background)
val GrayText = Color(0xFF9CA3AF)       // Cool Gray (muted text)
val ErrorRed = Color(0xFFEF5350)       // Coral Red (errors)
val SurfaceDark = Color(0xFF1E293B)    // Dark Slate (dark surface)

// Additional palette shades
val WarmRose = Color(0xFFE991A9)       // Warm Rose (replaces amber warnings)
val LightBlueGlow = Color(0xFFD6EBFF)  // Pale sky blue glow
val PinkMist = Color(0xFFFCE4EC)       // Very light pink tint
val IceWhite = Color(0xFFFFFFFF)       // Pure white (light surface)
